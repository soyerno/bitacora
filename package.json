{
  "name": "erno-modo",
  "private": true,
  "type": "module",
  "description": "Static site (erno-modo) — specs + tests for the vanilla-JS catalog.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:specs": "vitest run tests/specs"
  },
  "devDependencies": {
    "vitest": "^2.1.9",
    "happy-dom": "^15.11.7"
  }
}
